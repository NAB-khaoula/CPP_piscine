#ifndef WHATEVER_HPP
#define WHATEVER_HPP
#include <iostream>


template <typename arg>
void    swap(arg&, arg&);
template <typename arg>
arg     min(arg, arg);
template <typename arg>
arg		max(arg, arg);

#endif